name: Release
on:
  push:
#    tags:
#      - v*.*.*
  pull_request:


jobs:
  build:
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true
    steps:
    - name: Print SUCCESS
      env:
        COMMIT_MSG: ${{ github.event.head_commit.message }}
        BRANCH: ${{ github.ref }}
        GITHUB_CONTEXT: ${{ toJson(github) }}
        ACTION: ${{ toJson(github.action) }}
        MERGE_STATUS: ${{ toJson(github.pull_request.merged) }}

      run: |
        echo "$BRANCH"
        echo "$GITHUB_CONTEXT"
        echo "<------->"

  more:
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == false
    steps:
    - name: Print false
      env:
        COMMIT_MSG: ${{ github.event.head_commit.message }}
        BRANCH: ${{ github.ref }}
        GITHUB_CONTEXT: ${{ toJson(github) }}
        PRs: ${{ toJson(github.event.pull_request) }}
        LABELS: ${{ toJson(github.event.pull_request.labels) }}
        NAMES: ${{ toJson(github.event.pull_request.labels.name) }}
        ACTION: ${{ toJson(github.action) }}
        MERGE_STATUS: ${{ toJson(github.event.pull_request.merged) }}

      run: |
        echo "----run it----"
        echo "full context = $GITHUB_CONTEXT"
        echo "commit msg = $COMMIT_MSG"
        echo "Branch = $BRANCH"
        echo "Ref = $GITHUB_REF"
        echo "labels = $LABELS"
        echo "EVENTs = $EVENT"
        echo "PRs = $PRs"
        echo "NAMEs = $NAMES"
        echo "ACTIONs = $ACTION"
        echo "MERGE_STATUSes = $MERGE_STATUS"

  final:
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true
    steps:
    - name: Print false
      run: echo "true its true and lets say  can this work without a tag?"


#  OK asdfasdfsa