name: Python
on:
  push:
    tags:
      - v*.*.*
  pull_request:

jobs:
  py:
    steps:
      - name: Display the path
        run: |
          import os
          print(os.environ['PATH'])
          pip install requests
          print(requests)
        shell: python
