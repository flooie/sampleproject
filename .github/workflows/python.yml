name: Python
on:
  push:

jobs:
  py:
    steps:
      - name: Display the path
        run: |
          import os
          print(os.environ['PATH'])
          pip install requests
          print(requests)
        shell: python
